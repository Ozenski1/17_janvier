require_relative "../lib/tow.rb"

describe "the perform method" do
  it "should return an array with the townhalls' names and emails" do
    expect(perform).not_to be_nil
  end
end


describe "the perform method" do
  it "should return an array with the townhalls' names and emails" do
    expect(get_townhall_urls).not_to be_nil
  end
end

describe "the perform method" do
  it "should return an array with the townhalls' names and emails" do
    expect(get_townhall_email).not_to be_nil
  end
end
